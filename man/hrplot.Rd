% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazardratio_plot.R
\name{hrplot}
\alias{hrplot}
\title{Hazard Ratio Plot}
\usage{
hrplot(data, var, lim0 = 0, lim1 = 1500)
}
\arguments{
\item{data}{A \code{data.frame} containing hazard ratio results. Must include columns:
\itemize{
\item \code{time}: time points
\item \code{loghr}: log hazard ratio
\item \code{low}: lower bound of log hazard ratio
\item \code{up}: upper bound of log hazard ratio
}}

\item{var}{Character. Name of the treatment group (used for plot title).}

\item{lim0}{Numeric. Minimum x-axis limit (default 0).}

\item{lim1}{Numeric. Maximum x-axis limit (default 1500).}
}
\value{
A \code{ggplot} object showing the hazard ratio over time, with solid line for
log(HR) and dashed lines for confidence intervals.
}
\description{
Creates a line plot of hazard ratio (HR) over time, including pointwise
estimates and confidence intervals.
}
\details{
The function reshapes the input \code{data} from wide to long format using \code{tidyr::gather()},
then creates a line plot using \code{ggplot2}, with separate line types for log(HR)
and its confidence bounds. A horizontal dashed line is added at 0 for reference.
}
\examples{
\dontrun{
# Suppose bshr() has been used to create HR data
boot_hr <- bshr(data=example_data, contr = "Control", var = "GTE", n_boot = 100)
p <- hrplot(boot_hr, var = "GTE")
print(p)
}
}
\keyword{hazard}
\keyword{plot}
\keyword{ratio}
