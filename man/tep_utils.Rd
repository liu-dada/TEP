% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{tep_utils}
\alias{tep_utils}
\alias{days_to_months}
\alias{pct_change}
\alias{km_summaries_days}
\title{Utility Helpers for Time, Percent Change, and KM Summaries}
\usage{
days_to_months(x)

pct_change(tv, cv)

km_summaries_days(df)
}
\arguments{
\item{x}{Numeric vector of time values in days.}

\item{tv}{Numeric. Treatment value.}

\item{cv}{Numeric. Control value.}

\item{df}{A data frame containing at least \code{age}, \code{dead}, and \code{group}.}
}
\value{
\itemize{
\item \code{days_to_months()} returns a numeric vector in months.
\item \code{pct_change()} returns a numeric percent change.
\item \code{km_summaries_days()} returns a data frame with one row per group.
}
}
\description{
These small helper functions provide convenience utilities for:
}
\details{
\itemize{
\item converting time from days to months,
\item computing percent change between two values,
\item summarizing Kaplan–Meier survival results by group (median survival and maximum observed event time).
}

They are used internally for reporting and plotting survival-based summaries.

\code{km_summaries_days()} fits a Kaplan–Meier model by group and returns:
\itemize{
\item median survival time with confidence limits (in days),
\item maximum observed event time (among subjects with events).
}
}
\examples{
days_to_months(365)

pct_change(tv = 120, cv = 100)

\dontrun{
set.seed(123)
dat <- data.frame(
  age   = runif(200, 1, 1500),
  dead  = rbinom(200, 1, 0.4),
  group = sample(c("Control", "Treatment"), 200, replace = TRUE)
)

km_summaries_days(dat)
}

}
