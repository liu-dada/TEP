% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazardSE_asymptotic.R
\name{hazardSE}
\alias{hazardSE}
\title{Calculate Hazard Ratio and Standard Error Over Time}
\usage{
hazardSE(data, contr, var)
}
\arguments{
\item{data}{A \code{data.frame} containing survival data. Must include at least
\code{age}, \code{dead}, and \code{group} columns.}

\item{contr}{Character. Name of the control group in \code{group} column.}

\item{var}{Character. Name of the treatment group in \code{group} column.}
}
\value{
A \code{data.frame} with columns:
\itemize{
\item \code{time}: time points for hazard evaluation
\item \code{h0}: hazard for control group
\item \code{se0}: standard error for control hazard
\item \code{h1}: hazard for treatment group
\item \code{se1}: standard error for treatment hazard
\item \code{hr}: hazard ratio (h1 / h0)
\item \code{loghr}: log hazard ratio
\item \code{var}: variance of log HR
\item \code{low}: lower 95\% confidence bound for log HR
\item \code{up}: upper 95\% confidence bound for log HR
\item \code{name}: name of the treatment group
}
}
\description{
This function computes the asymptotic hazard ratio (HR) between a treatment group
and a control group over time, including standard errors and confidence intervals,
using the \code{safe_bshazard()} function to estimate hazard functions.
}
\details{
The function first estimates the hazard function for each group using
\code{safe_bshazard()}, then interpolates hazard estimates to common time points,
computes log-transformed standard errors, and finally calculates the hazard ratio
with asymptotic variance and confidence intervals.
}
\examples{
\dontrun{
hr_df <- hazardSE(data=example_data, contr = "Control", var = "GTE")
head(hr_df)
}
}
\keyword{hazard}
\keyword{ratio}
\keyword{survival}
